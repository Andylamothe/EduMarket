<UserControl x:Class="WpfApplication.page.Catalogue"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApplication.page"
             mc:Ignorable="d" 
             d:DesignHeight="700" 
             d:DesignWidth="1200"
             Background="#FF2C3E50"
             >
    <Grid Background="#FF2C3E50">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
           Text="Catalogue"
           Foreground="#F4C095"
           FontSize="50" 
           FontWeight="Bold" 
           Margin="20,20,20,0"
           HorizontalAlignment="Center"/>


        <Grid Grid.Row="1" Margin="0,10,0,0" Width="1500" Height="800">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <ListBox ItemsSource="{Binding CatalogueItems}"
                 SelectedItem="{Binding SelectedItem}"
                 Background="#748CAB">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="0,0,0,1"
                            BorderBrush="#FF2C3E50"
                            Width="1480">
                                <Grid Height="180" Width="1100">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Image Source="/Assets/image-alt-regular-24.png"
                                   Width="150"
                                   Grid.Column="0"
                                   Margin="-300,0,0,0"/>

                                    <TextBlock Text="{Binding ItemName}"
                                       Foreground="#F4C095"
                                       FontWeight="Bold"
                                       FontSize="40"
                                       VerticalAlignment="Center"
                                       Grid.Column="1"/>

                                    <TextBlock Text="{Binding Price, StringFormat='{}{0} $'}"
                                       Foreground="Black"
                                       FontWeight="Bold"
                                       FontSize="40"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Right"
                                       Grid.Column="2"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
