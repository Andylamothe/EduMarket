<UserControl x:Class="WpfApplication.page.CatalogueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApplication.page"
             mc:Ignorable="d" 
             d:DesignHeight="700" 
             d:DesignWidth="1200"
             Background="#FF2C3E50"
             >
    <Grid Background="#FF2C3E50" Cursor="">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
           Text="Catalogue"
           Foreground="#F4C095"
           FontSize="50" 
           FontWeight="Bold" 
           Margin="20,20,20,0"
           HorizontalAlignment="Center"/>
        <Button
            Grid.Row="0"
            Content="Load"
            Height="40"
            Width="200"
            Margin="0,30,30,0"
            Background="#FF3498DB"
            FontSize="15"
            FontWeight="Bold"
            Foreground="#F4C095"
            HorizontalAlignment="Right"
            Cursor="Hand"
            Command="{Binding ReloadItemsAsyncCommand}"/>

        <Grid Grid.Row="1" Margin="0,10,0,0" Width="1500" Height="800">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                
                

                <ListBox ItemsSource="{Binding Items}"
                 Background="#748CAB">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="0,0,0,1"
                            BorderBrush="#FF2C3E50"
                            Width="1480">
                                <Grid Height="180" Width="1100">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Image Source="/Assets/image-alt-regular-24.png"
                                   Width="150"
                                   Grid.Column="0"
                                   Margin="-300,0,0,0"/>

                                    <TextBlock Text="{Binding Name}"
                                       Foreground="#F4C095"
                                       FontWeight="Bold"
                                       FontSize="40"
                                       VerticalAlignment="Center"
                                       Grid.Column="0"/>

                                    <TextBlock Text="{Binding Description}"
                                       Foreground="Black"
                                       FontWeight="Bold"
                                       FontSize="40"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Right"
                                       Grid.Column="1"/>

                                    <TextBlock Text="{Binding Price, StringFormat='{}{0} $'}"
                                       Foreground="Black"
                                       FontWeight="Bold"
                                       FontSize="40"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Right"
                                       Grid.Column="2"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
